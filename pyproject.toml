[project]
name = "fundmycampus-crm"
version = "1.0.0"
description = "FundMyCampus CRM Backend — Education Loan Consultancy Lead Management"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
